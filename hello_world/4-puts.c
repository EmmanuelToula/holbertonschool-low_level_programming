#include <stdio.h>

/**
 * main - puts a string with special character.
 *
 * Description: Prints a message using puts
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
